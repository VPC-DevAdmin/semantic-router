# Tier 4 — tier metadata only. Callable models come from `.env`'s
# TIER4_{i}_* slots (vendor APIs; no local launch).

name: tier4
level: 4
specializations: [general, coding, math, reasoning, creative_writing]
timeout_s: 180
router_alias: tier4
