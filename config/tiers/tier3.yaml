# Tier 3 — tier metadata only. Callable models come from `.env`'s
# TIER3_{i}_* slots (vendor APIs; no local launch).

name: tier3
level: 3
specializations: [general, coding, math, creative_writing]
timeout_s: 120
router_alias: tier3
