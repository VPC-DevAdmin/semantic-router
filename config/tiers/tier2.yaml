# Tier 2 — tier metadata only. Local launch recipe lives in
# config/local_models.yaml keyed by `.env`'s TIER2_1_MODEL.

name: tier2
level: 2
specializations: [general, coding]
# Qwen3-30B-A3B MoE on CPU; long code-gen prompts (e.g. "implement a
# Bloom filter") can run 2-4 min. 300s headroom.
timeout_s: 300
router_alias: tier2
