# Tier 5 — tier metadata only. Callable models come from `.env`'s
# TIER5_{i}_* slots (vendor APIs; no local launch).
# This is also the GOLD tier — `make answers` skips top-tier-routed
# queries; `make update-gold` calls every TIER5 slot to produce
# per-provider expected answers.

name: tier5
level: 5
specializations: [general, coding, math, reasoning, creative_writing]
timeout_s: 240
router_alias: tier5
